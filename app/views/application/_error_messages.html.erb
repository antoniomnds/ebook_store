<% if object.errors.any? %>
  <div class="alert alert-danger">
    <h2>
      <%= pluralize(object.errors.count, "error") %> prohibited this <%= object.class.to_s.downcase %>
      from being saved:
    </h2>

    <ul>
      <% object.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
